# Name of your application. Used to uniquely configure containers.
service: newsbox

# Name of the container image.
image: cjgilbert/newsbox

# Deploy to these servers.
#
# Whichever host this lands on needs an egress rule blocking outbound
# traffic from the app container to private, loopback and link-local
# addresses. Ingest fetches newsletter images, so senders choose the URLs
# this app requests. Kamal will not install the rule and nothing here can —
# see "Outbound network" in README.md.
servers:
  web:
    - 46.224.179.132
  # job:
  #   hosts:
  #     - 192.168.0.1
  #   cmd: bin/jobs

# Enable SSL auto certification via Let's Encrypt and allow for multiple apps on a single web server.
# Remove this section when using multiple web servers and ensure you terminate SSL at your load balancer.
#
# Note: If using Cloudflare, set encryption mode in SSL/TLS setting to "Full" to enable CF-to-app encryption.
proxy:
  ssl: true
  host: siftbox.co

# Credentials for your image host.
registry:
  # Docker Hub, which is where the other apps on this host come from too.
  username: cjgilbert

  # Always use an access token rather than real password when possible.
  password:
    - KAMAL_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    # Also decrypts action_mailbox.ingress_password, which is what Action
    # Mailbox authenticates the Postmark webhook against. Without this the
    # ingress rejects every delivery and newsletters are lost once Postmark
    # gives up retrying.
    - RAILS_MASTER_KEY
    # Outbound mail, for password reset. The only account-recovery path.
    - POSTMARK_SMTP_TOKEN
  clear:
    # The address subscriptions are pointed at, shown in the feed header and
    # the empty state. Without it, a deployed newsbox tells the reader to
    # subscribe to example.com.
    NEWSBOX_INBOUND_ADDRESS: news@news.siftbox.co

    # The feed groups by calendar day, so this decides where the day breaks.
    NEWSBOX_TIME_ZONE: London

    # The host password-reset links are built against.
    NEWSBOX_HOST: siftbox.co

    # The From address on password-reset mail. Must be a Postmark sender
    # signature, or Postmark rejects the send.
    NEWSBOX_MAIL_FROM: newsbox@siftbox.co

    # Run the Solid Queue Supervisor inside the web server's Puma process to do jobs.
    # When you start using multiple servers, you should split out job processing to a dedicated machine.
    SOLID_QUEUE_IN_PUMA: true

    # Set number of processes dedicated to Solid Queue (default: 1)
    # JOB_CONCURRENCY: 3

    # Set number of cores available to the application on each server (default: 1).
    # WEB_CONCURRENCY: 2

    # No DB_HOST and no db accessory: the databases are SQLite files on the
    # volume below, alongside the Active Storage blobs.

    # Log everything from Rails
    # RAILS_LOG_LEVEL: debug

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole"


# The whole of this app's state: the SQLite databases and the Active Storage
# blobs. Back this path up and you have backed up newsbox.
volumes:
  - "newsbox_storage:/rails/storage"


# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets

# Configure the image builder.
builder:
  arch: amd64

  # Built on the target host, which is amd64 already. The deploy machine is an
  # arm64 Mac, so building locally would go through emulation.
  remote: ssh://root@46.224.179.132

  #
  # # Pass arguments and secrets to the Docker build process
  # args:
  #   RUBY_VERSION: 3.3.6
  # secrets:
  #   - GITHUB_TOKEN
  #   - RAILS_MASTER_KEY

# Use a different ssh user than root
# ssh:
#   user: app

# Use accessory services (secrets come from .kamal/secrets).
# accessories:
#   db:
#     image: mysql:8.0
#     host: 192.168.0.2
#     # Change to 3306 to expose port to the world instead of just local network.
#     port: "127.0.0.1:3306:3306"
#     env:
#       clear:
#         MYSQL_ROOT_HOST: '%'
#       secret:
#         - MYSQL_ROOT_PASSWORD
#     files:
#       - config/mysql/production.cnf:/etc/mysql/my.cnf
#       - db/production.sql:/docker-entrypoint-initdb.d/setup.sql
#     directories:
#       - data:/var/lib/mysql
#   redis:
#     image: redis:7.0
#     host: 192.168.0.2
#     port: 6379
#     directories:
#       - data:/data
