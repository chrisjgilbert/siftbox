#!/usr/bin/env bash
# Runs the processes in Procfile.dev: the web server, and the Solid Queue
# worker that Action Mailbox needs to process inbound email.
set -e

trap 'kill 0' EXIT INT TERM

bin/rails server "$@" &
bin/jobs &

wait -n
