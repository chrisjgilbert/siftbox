<%# The pen notice: app chrome, drawn above everything the editor wrote and
    outside the edition entirely, so nothing about it can read as a story.

    One rule of its own underneath, and the masthead's above it. A second rule
    on top would sit flush against that one and read as a single 2px rule —
    the one weight this band is not. %>
<%= link_to badge.line, badge.path, class: "badge" %>
