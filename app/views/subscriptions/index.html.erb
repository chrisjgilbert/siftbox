<%= render "application/masthead", line: t(".line") %>

<main class="pen">
  <% @subscriptions.sections.each do |section| %>
    <%= render "subscriptions/section", section: section %>
  <% end %>

  <%= render "blogs/section", blog: @subscriptions.blog, sources: @subscriptions.sources %>
</main>
