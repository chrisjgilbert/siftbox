<%# One paragraph of the editor's copy. The name comes from the block rather
    than from a conditional here, so the story partial never asks what it is
    drawing. %>
<p class="story__paragraph"><%= block.text %></p>
