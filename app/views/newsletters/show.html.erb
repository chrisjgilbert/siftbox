<% content_for :title, @newsletter.subject %>

<header class="topbar">
  <%= link_to t("newsletters.show.back"), newsletters_path, class: "topbar__back" %>

  <div class="topbar__actions">
    <%= button_to t("newsletters.show.mark_unread"),
          newsletter_read_path(@newsletter),
          method: :delete,
          class: "topbar__action",
          form_class: "topbar__form" %>
    <%= link_to t("newsletters.show.view_original"),
          newsletter_original_path(@newsletter),
          class: "topbar__action" %>
  </div>
</header>

<article class="article">
  <p class="article__sender">
    <span class="article__sender-name"><%= @newsletter.sender %></span>
    — <%= @newsletter.sender_domain %>
  </p>

  <h1 class="article__title"><%= @newsletter.subject %></h1>

  <p class="article__received"><%= @newsletter.received_line %></p>

  <hr class="article__rule">

  <div class="body"><%= newsletter_body(@newsletter) %></div>

  <nav class="neighbours">
    <div class="neighbours__cell neighbours__cell--newer">
      <% if @newsletter.newer %>
        <p class="neighbours__label"><%= t("newsletters.show.newer") %></p>
        <%= link_to @newsletter.newer.title,
              newsletter_path(@newsletter.newer),
              class: "neighbours__title" %>
      <% end %>
    </div>

    <div class="neighbours__cell neighbours__cell--older">
      <% if @newsletter.older %>
        <p class="neighbours__label"><%= t("newsletters.show.older") %></p>
        <%= link_to @newsletter.older.title,
              newsletter_path(@newsletter.older),
              class: "neighbours__title" %>
      <% end %>
    </div>
  </nav>
</article>
