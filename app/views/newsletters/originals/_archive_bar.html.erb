<%# The bar an original in the archive wears: the way back to the reader it
    was opened from, and nothing else. Held mail wears the pen's bar instead. %>
<header class="topbar">
  <%= link_to t("newsletters.original.back"),
        newsletter_path(newsletter),
        class: "topbar__back" %>
</header>
